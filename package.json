{
  "name": "Noys",
  "version": "0.1.0",
  "description": "Test Parrot",
  "private": true,
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "scripts": {
    "browser-sync": "browser-sync start --files \"./**/static/**/*.css, ./**/static/**/*.js, ./**/**/*.py, ./**/templates/*.html\" --proxy 127.0.0.1:8000 --reload-delay=3000 --reload-debounce=500",
    "start": "concurrently --kill-others \"npm run browser-sync\""
  },
  "dependencies": {},
  "devDependencies": {
    "browser-sync": "^2.26.12",
    "concurrently": "^5.3.0"
  }
}
